{
  "name": "kehila-rules-tests",
  "private": true,
  "scripts": {
    "test": "jest",
    "test:rules": "firebase emulators:exec --only firestore,storage \"jest\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^10.12.0",
    "firebase-tools": "^13.0.0",
    "jest": "^29.7.0"
  }
}
