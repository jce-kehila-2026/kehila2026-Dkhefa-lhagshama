{
  "name": "kehila2026-dkhefa-lhagshama",
  "version": "0.1.0",
  "private": true,
  "description": "Push for Fulfillment — community platform monorepo (frontend + backend + Firebase rules).",
  "scripts": {
    "test:rules": "npm --prefix tests run test:rules",
    "test:rules:nojest": "echo \"Boots the Firestore + Storage emulators, then runs jest. Requires Java 11+ and firebase-tools.\""
  }
}
