{
  "name": "kehila2026-dkhefa-lhagshama",
  "version": "0.1.0",
  "private": true,
  "description": "Push for Fulfillment — community platform monorepo (frontend + backend + Firebase rules).",
  "scripts": {
    "test:rules": "firebase emulators:exec --config firebase.emulators.json --project demo-test --only firestore,storage \"npm test --prefix tests\""
  }
}
