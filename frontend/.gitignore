# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files — ignore ALL env files except the documented example.
# (NEXT_PUBLIC_* values are public client config, not secrets, but env files
# still don't belong in git: provide them via .env*.local or CI env vars.)
.env*
!.env.example

# typescript
*.tsbuildinfo
next-env.d.ts.bak
next-env.d.ts
