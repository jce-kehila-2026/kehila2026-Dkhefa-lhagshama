/**
 * Workstream A (public polish) translation add-ons — home, directory, volunteer.
 * Owned by the public-polish work. Add keys under `he` and `en` mirrors.
 * Merged into the main `t.*` tree by `translations.ts`.
 */
export const overhaulPublic = {
  he: {},
  en: {},
} as const;
