# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env
.env.local
.env.*.local
*.local

# Firebase — never commit service-account JSONs or local cache
.firebase/
*-firebase-adminsdk-*.json
backend/serviceAccountKey.json
backend/privateKey.json
# NOTE: `.firebaserc` IS committed — it's the public project-id map, not a secret.


# Build artifacts
.next/
out/
coverage/
# Firebase Agent Skills (local AI-tool config � not for team repo)
.agents/
skills-lock.json

# AI-assistant artifacts — kept locally for context, never tracked in the repo.
# (CLAUDE.md matches at any depth: root, frontend/, backend/.)
CLAUDE.md
.claude/

# Superpowers brainstorm/visual companion artifacts (local only)
docs/superpowers/
.superpowers/

# Session work-reports / feedback-round artifacts — context only, live in the
# sem2-workspace repo, never in the team repo (e.g. PROFESSOR-FEEDBACK-ROUND-*,
# FEEDBACK-ROUND-*, NPO-ROUND-*, and their E2E screenshot dirs).
*-ROUND-*/
*FEEDBACK-ROUND*/
e2e-screenshots/
# Auto-generated command message logs
2026-*-command-message*.txt

# Local test-account credentials (do not commit plaintext passwords)
TEST-ACCOUNTS.md

# Leftover `gh issue list` dumps (working artifacts, never a tracked source)
open_issues.json

# design-sync generated/staged output (the .design-sync/ source is tracked; its
# build products are not).
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
frontend/.ds-build/
frontend/tsconfig.sync.json
.ds-pkg/
.ds-nm/
